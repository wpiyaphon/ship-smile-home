import { HomeView } from 'src/sections/ship-home/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'ShipSmile: Home',
};

export default function OverviewAppPage() {
  return <HomeView />;
}
