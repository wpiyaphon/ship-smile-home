import { HomeView } from 'src/sections/home/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Ship Smile Services',
};

export default function HomePage() {
  return <HomeView />;
}
